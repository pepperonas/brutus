package com.pepperonas.brutus.ui.theme

import androidx.compose.ui.graphics.Color

// Primary accent — used for buttons, TimePicker thumb, FAB, selected chips.
// A little darker than before to feel less neon and more serious.
val BrutusRed = Color(0xFFE53935)           // Material Red 600

// Brighter variant for TEXT on dark backgrounds (OutlinedButton labels,
// error messages, inline accents) — gives better contrast than the
// primary accent without blowing out.
val BrutusRedBright = Color(0xFFFF8A80)     // Material Red A100

val BrutusDarkRed = Color(0xFFB71C1C)       // Material Red 900 — gradient bg only
val BrutusOrange = Color(0xFFFF9800)
val BrutusOrangeBright = Color(0xFFFFB74D)

val BrutusDark = Color(0xFF121212)
val BrutusSurface = Color(0xFF1E1E1E)
val BrutusCard = Color(0xFF2C2C2C)
val BrutusTextPrimary = Color(0xFFEEEEEE)
val BrutusTextSecondary = Color(0xFFB0B0B0)
