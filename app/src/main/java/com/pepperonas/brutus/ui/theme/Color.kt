package com.pepperonas.brutus.ui.theme

import androidx.compose.ui.graphics.Color

// Brighter, high-contrast reds so text stays readable on dark surfaces.
val BrutusRed = Color(0xFFFF5252)           // Material Red A200 — buttons, accents, icons
val BrutusRedBright = Color(0xFFFF8A80)     // Material Red A100 — text on dark
val BrutusDarkRed = Color(0xFFD32F2F)       // Material Red 700 — gradient background only
val BrutusOrange = Color(0xFFFF9800)        // Brighter than 6D00 for text contrast
val BrutusOrangeBright = Color(0xFFFFB74D)  // Secondary progress / snooze text

val BrutusDark = Color(0xFF121212)
val BrutusSurface = Color(0xFF1E1E1E)
val BrutusCard = Color(0xFF2C2C2C)
val BrutusTextPrimary = Color(0xFFEEEEEE)
val BrutusTextSecondary = Color(0xFFB0B0B0)   // brighter than 9E9E9E for better contrast
